<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Launch screen theme (shown before Flutter loads) -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Make the launch screen completely white -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- Normal theme after Flutter starts -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="ImpactAlertTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:colorAccent">#2E7D32</item>
    </style>

</resources>
